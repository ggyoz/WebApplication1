@model CSR.Models.User
@using Microsoft.AspNetCore.Mvc.Localization
@inject IStringLocalizer<CSR.SharedStrings> SharedLocalizer

<tr style="cursor: pointer;" onclick="window.location.href = '@Url.Action("Details", "User", new { id = Model.UserId })';">
    <td>@Html.DisplayFor(modelItem => modelItem.UserId)</td>
    <td>@Html.DisplayFor(modelItem => modelItem.UserName)</td>
    <td>@Html.DisplayFor(modelItem => modelItem.CorpName)</td>
    <td>@Html.DisplayFor(modelItem => modelItem.DeptName)</td>
    <td>@Html.DisplayFor(modelItem => modelItem.OfficeName)</td>
    <td>@Html.DisplayFor(modelItem => modelItem.TeamName)</td>
    <td>@Html.DisplayFor(modelItem => modelItem.EmailAddr)</td>
    <td>@Html.DisplayFor(modelItem => modelItem.UseYn)</td>
    <td>@Html.DisplayFor(modelItem => modelItem.UserDiv)</td>
    <!-- <td>@Model.RegDate.ToString("yyyy-MM-dd")</td> -->
    <td>
        <div class="btn-group btn-group-sm" onclick="event.stopPropagation();">
            <a asp-action="Edit" asp-route-id="@Model.UserId" class="btn btn-sm btn-outline-secondary">@SharedLocalizer["Button_Edit"]</a>
            <!-- <a asp-action="Delete" asp-route-id="@Model.UserId" class="btn btn-sm btn-outline-secondary">@SharedLocalizer["Button_Delete"]</a> -->
        </div>
    </td>
</tr>
